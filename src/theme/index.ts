import { createTheme } from "@mui/material";
import { appPalette as palette } from "./palette";
import { typography } from "./typography";

export const theme = createTheme({
  palette,
  typography,
});
